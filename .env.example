# === Example Environment Files ===

# For multiple display environments, you can specify the display to use
# for the running application. This is useful when you have multiple
# monitors or when running the application in a headless environment.
DISPLAY = ":0"
